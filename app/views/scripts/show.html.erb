<%= render partial: 'script', locals: { script: @script } %>

<%= link_to 'Edit Script', edit_script_path(@script), class: 'button button-primary' if can?(:manage, Script) %>
<%= link_to 'See All Scripts', scripts_path, class: 'button' %>
