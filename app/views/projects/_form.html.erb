<%= form_for(@project, remote: true, html: { class: :form, id: :project }) do |f| %>
  <%= f.text_field :name %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.submit 'Create Project' %>
<% end %>