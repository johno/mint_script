<%= link_to gravatar_image_tag(user.email, alt: user.email, gravatar: { secure: true }), user_path(user) %><br>
<%= link_to user.email, user_path(user) %>
<%= current_user.following?(user) ? unfollow_link(user.id) : follow_link(user.id) if current_user %>